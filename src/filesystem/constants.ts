/**
 * Special names that cannot be used for files or folders except in special circumstances.
 */
export const DISALLOWED_CONTENT_NAMES = ['.', '..', '/', ''];

/**
 * The filesystem's path separator. This is used to tokenize paths.
 */
export const PATH_SEPARATOR = '/';
